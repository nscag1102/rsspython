[project]
name = "rsspython"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.5",
    "pydantic>=2.0.0",
    "feedparser>=6.0.0",
    "pytest>=9.0.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.1",
]
